import { Router } from "express"; 
import * as {{camelCase name}}Controller from "./{{dashCase name}}.controller"; 

const router = Router(); router.get("/",{{camelCase name}}Controller.testController); 

export default router;