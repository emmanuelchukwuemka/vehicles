import { z } from "zod";

// Am creating validation schema for {{pascalCase name}} here
export const {{camelCase name}}Schema = z.object({

  // Example 
  //name: z.string().min(2, "Name is required").max(50, "Name is too long"),

});

// Inferred TypeScript type from schema
export type {{pascalCase name}}Input = z.infer<typeof {{camelCase name}}Schema>;
